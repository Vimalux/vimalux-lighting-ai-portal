import React, { useEffect, useMemo, useRef, useState } from "react";
import * as XLSX from "xlsx";
import { auditRowsToManualGroups, calculateManualProject, createManualGroup, recommendProduct } from "./v31ManualProjectEngine.js";

const DEFAULT_PRODUCTS = [
  { id:"street20", name:"VIMALUX Street 20", category:"LED Streetlights", watt:20, lumen:3200, buyPrice:85, sellPrice:155, active:true },
  { id:"street30", name:"VIMALUX Street 30", category:"LED Streetlights", watt:30, lumen:4800, buyPrice:92, sellPrice:165, active:true },
  { id:"street40", name:"VIMALUX Street 40", category:"LED Streetlights", watt:40, lumen:6400, buyPrice:100, sellPrice:175, active:true },
  { id:"street60", name:"VIMALUX Street 60", category:"LED Streetlights", watt:60, lumen:9600, buyPrice:110, sellPrice:190, active:true },
  { id:"road90", name:"VIMALUX Road 90", category:"LED Streetlights", watt:90, lumen:14400, buyPrice:150, sellPrice:210, active:true },
  { id:"highway120", name:"VIMALUX Highway 120", category:"LED Streetlights", watt:120, lumen:19200, buyPrice:205, sellPrice:285, active:true },
  { id:"highway150", name:"VIMALUX Highway 150", category:"LED Streetlights", watt:150, lumen:24000, buyPrice:230, sellPrice:320, active:true },
];

const DEFAULTS = { cloSavingPct:10, smartSolutionSavingPct:0, maintenanceOldPerLamp:25, powerAidAdditionalSavingPct:40, energyPrice:0.29, burningHours:4200, smartNodeCost:62, cmsFeePerLampYear:6, powerAidFeePerLampYear:0, hybridProductionKwhPerLampYear:70, hybridAdditionalCapexPerLamp:0, analysisYears:20, savingIndexationPct:0, discountRatePct:6, performanceDegradationPct:0, co2KgPerKwh:0.233, sapBallastFactor:1.2, mhBallastFactor:1.15, mercuryBallastFactor:1.15, fluorescentBallastFactor:1.1, ledBallastFactor:1, unknownBallastFactor:1 };
const integer = new Intl.NumberFormat("it-IT", { maximumFractionDigits:0 });
const decimal = new Intl.NumberFormat("it-IT", { minimumFractionDigits:2, maximumFractionDigits:2 });
const one = new Intl.NumberFormat("it-IT", { minimumFractionDigits:1, maximumFractionDigits:1 });
const money = v => `${integer.format(Math.round(Number(v)||0))} €`;
const money2 = v => `${decimal.format(Number(v)||0)} €`;
const number = (v,d=0) => d===2 ? decimal.format(Number(v)||0) : d===1 ? one.format(Number(v)||0) : integer.format(Number(v)||0);
const num = value => { const raw=String(value??"").trim().replace(/\s/g,""); if(!raw)return 0; const normalized=raw.includes(",")?raw.replace(/\./g,"").replace(",","."):raw; return Number(normalized)||0; };
const payment = (principal,annualRate,years) => { const months=Math.max(1,Math.round(years*12)); const rate=(annualRate/100)/12; return rate===0?principal/months:principal*rate*Math.pow(1+rate,months)/(Math.pow(1+rate,months)-1); };

export default function AppV31Complete(){
  const [section,setSection]=useState("overview");
  const [viewMode,setViewMode]=useState("internal");
  const [quoteDialog,setQuoteDialog]=useState(false);
  const [quoteLanguage,setQuoteLanguage]=useState("it");
  const [project,setProject]=useState({municipality:"Comune di Larciano",quotationId:"Q-2026-001"});
  const [products,setProducts]=useState(()=>{ try{return JSON.parse(localStorage.getItem("vimalux-products"))||DEFAULT_PRODUCTS;}catch{return DEFAULT_PRODUCTS;} });
  const [assumptions,setAssumptions]=useState(DEFAULTS);
  const [commercial,setCommercial]=useState({model:"Noleggio Operativo",years:9,interestRate:8,upfront:0,installationSellPerLamp:30,installationCostPerLamp:25,freightSellPerLamp:11,freightCostPerLamp:11,maintenanceAfterPerLamp:15,smartHardwareSellPerLamp:62,smartHardwareCostPerLamp:30,cmsSellPerLampYear:6,cmsCostPerLampYear:3.42,powerAidPerformanceSharePct:10,powerAidCostPerLampYear:0.24,commissionPct:0,commissionBasis:"Luminaires",bonusPct:8,financeSellOffPct:8,overallPriceAdjustment:0});
  const [groups,setGroups]=useState([createManualGroup(1,{label:"Street lighting",quantity:682,existingType:"SAP",existingWatt:87,productId:"street30",smart:true,powerAid:true,hybrid:false})]);
  const auditRef=useRef(null);
  useEffect(()=>localStorage.setItem("vimalux-products",JSON.stringify(products)),[products]);
  const activeProducts=useMemo(()=>products.filter(p=>p.active!==false),[products]);
  const calc=useMemo(()=>calculateManualProject(groups,assumptions,activeProducts),[groups,assumptions,activeProducts]);
  const productMap=useMemo(()=>new Map(products.map(p=>[p.id,p])),[products]);

  const quote=useMemo(()=>{
    const qty=calc.totals.quantity;
    const lumSell=groups.reduce((s,g)=>s+num(g.quantity)*num(productMap.get(g.productId)?.sellPrice),0);
    const lumCost=groups.reduce((s,g)=>s+num(g.quantity)*num(productMap.get(g.productId)?.buyPrice),0);
    const smartQty=groups.reduce((s,g)=>s+(g.smart?num(g.quantity):0),0);
    const powerQty=groups.reduce((s,g)=>s+(g.smart&&g.powerAid?num(g.quantity):0),0);
    const smartSell=smartQty*commercial.smartHardwareSellPerLamp, smartCost=smartQty*commercial.smartHardwareCostPerLamp;
    const installSell=qty*commercial.installationSellPerLamp, installCost=qty*commercial.installationCostPerLamp;
    const freightSell=qty*commercial.freightSellPerLamp, freightCost=qty*commercial.freightCostPerLamp;
    const cmsSell=smartQty*commercial.cmsSellPerLampYear*commercial.years, cmsCost=smartQty*commercial.cmsCostPerLampYear*commercial.years;
    const powerAidAnnualGrossSaving=calc.totals.powerAidSavingKwh*assumptions.energyPrice;
    const powerAidAnnualFee=powerAidAnnualGrossSaving*commercial.powerAidPerformanceSharePct/100;
    const powerSell=powerAidAnnualFee*commercial.years, powerCost=powerQty*commercial.powerAidCostPerLampYear*commercial.years;
    const powerAidCustomerAnnualBenefit=Math.max(0,powerAidAnnualGrossSaving-powerAidAnnualFee);
    const cashPrice=Math.max(0,lumSell+smartSell+installSell+freightSell+cmsSell+powerSell+commercial.overallPriceAdjustment);
    const principal=Math.max(0,cashPrice-commercial.upfront); const monthly=payment(principal,commercial.interestRate,commercial.years);
    const financedTotal=monthly*commercial.years*12+commercial.upfront, interest=financedTotal-cashPrice, annualPayment=monthly*12;
    const maintenanceSaving=Math.max(0,qty*assumptions.maintenanceOldPerLamp-qty*commercial.maintenanceAfterPerLamp);
    const customerAnnualSaving=calc.totals.totalEnergySavingValue+maintenanceSaving-powerAidAnnualFee, customerNet=customerAnnualSaving-annualPayment;
    const totalCost=lumCost+smartCost+installCost+freightCost+cmsCost+powerCost;
    const commission=(commercial.commissionBasis==="Total contract"?cashPrice:lumSell)*commercial.commissionPct/100;
    const bonus=(cashPrice-freightSell)*commercial.bonusPct/100, financeCost=interest*commercial.financeSellOffPct/100;
    const grossMargin=cashPrice-totalCost, mol1=grossMargin, mol2=mol1-commission-bonus, mol3=mol2-financeCost;
    const yearly=[]; let cumulative=0;
    for(let year=1;year<=20;year++){ const esc=Math.pow(1+assumptions.savingIndexationPct/100,year-1); const indexedEnergy=calc.totals.totalEnergySavingValue*esc; const powerAidFee=powerAidAnnualFee*esc; const totalSaving=indexedEnergy+maintenanceSaving-powerAidFee; const customerPayment=year<=commercial.years?annualPayment:0; const netCashflow=totalSaving-customerPayment; cumulative+=netCashflow; yearly.push({year,indexedEnergy,powerAidFee,maintenanceSaving,customerPayment,netCashflow,cumulative}); }
    return {qty,lumSell,lumCost,smartSell,smartCost,installSell,installCost,freightSell,freightCost,cmsSell,cmsCost,powerSell,powerCost,powerAidAnnualGrossSaving,powerAidAnnualFee,powerAidCustomerAnnualBenefit,cashPrice,monthly,annualPayment,financedTotal,interest,maintenanceSaving,customerAnnualSaving,customerNet,totalCost,commission,bonus,financeCost,grossMargin,mol1,mol2,mol3,yearly};
  },[calc,groups,productMap,assumptions,commercial]);

  const updateGroup=(id,key,value)=>setGroups(rows=>rows.map(r=>r.id===id?{...r,[key]:value}:r));
  const updateCommercial=(key,value)=>setCommercial(x=>({...x,[key]:value}));
  const updateProduct=(id,key,value)=>setProducts(rows=>rows.map(p=>p.id===id?{...p,[key]:value}:p));
  const addProduct=()=>setProducts(rows=>[...rows,{id:`product-${Date.now()}`,name:"New luminaire",category:"LED Streetlights",watt:40,lumen:6400,buyPrice:0,sellPrice:0,active:true}]);
  const duplicateProduct=id=>setProducts(rows=>{const p=rows.find(x=>x.id===id);return p?[...rows,{...p,id:`${p.id}-${Date.now()}`,name:`${p.name} copy`}]:rows;});
  const removeProduct=id=>setProducts(rows=>rows.filter(p=>p.id!==id));
  const applyRecommendation=id=>setGroups(rows=>rows.map(g=>{if(g.id!==id)return g;const r=recommendProduct(g.existingType,g.existingWatt,activeProducts);return {...g,productId:r.productId||g.productId};}));

  async function importAudit(event){ const file=event.target.files?.[0]; if(!file)return; const wb=XLSX.read(await file.arrayBuffer(),{type:"array"}); const sheet=wb.SheetNames.includes("Luminaire_Audit")?"Luminaire_Audit":wb.SheetNames[0]; const rows=XLSX.utils.sheet_to_json(wb.Sheets[sheet],{defval:""}); const normalized=rows.map((r,i)=>({label:r.Group||r.Name||r.ID||r.Pole_ID||`Group ${i+1}`,quantity:num(r.Quantity||r.Qty||1),existingType:r.Existing_Type||r.Technology||r.Tecnologia||r.Lamp_Type||r.Type||"Unknown",existingWatt:num(r.Existing_Watt||r.Wattage||r.Watt||r.Power)})).filter(r=>r.existingWatt>0); const imported=auditRowsToManualGroups(normalized,activeProducts); if(imported.length)setGroups(imported); setSection("audit"); event.target.value=""; }

  function generateQuotation(){ const it=quoteLanguage==="it"; const rows=quote.yearly.map(r=>`<tr><td>${r.year}</td><td>${money(r.indexedEnergy)}</td><td>${money(r.powerAidFee)}</td><td>${money(r.maintenanceSaving)}</td><td>${money(r.customerPayment)}</td><td>${money(r.netCashflow)}</td><td>${money(r.cumulative)}</td></tr>`).join(""); const html=`<!doctype html><html><head><meta charset="utf-8"><title>${project.quotationId}</title><style>body{font-family:Segoe UI,Arial;color:#133439;margin:0;background:#f3f6f8}.page{max-width:1050px;margin:24px auto;background:white;padding:42px}.head{display:flex;justify-content:space-between;border-bottom:4px solid #c9f15a;padding-bottom:18px}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.kpi{border:1px solid #dce6e7;border-radius:12px;padding:14px}.kpi b{display:block;font-size:20px;margin-top:7px}table{width:100%;border-collapse:collapse;font-size:12px}th,td{padding:9px;border-bottom:1px solid #e7eeee;text-align:right}th:first-child,td:first-child{text-align:left}.note{background:#eff8d8;padding:14px;border-radius:10px;margin:18px 0}</style></head><body><div class="page"><div class="head"><div><b style="font-size:24px">VIMALUX Intelligence</b><div>Infrastructure Transformation Partner</div></div><div><b>${project.quotationId}</b><br>${project.municipality}</div></div><h1>${it?"PROPOSTA COMMERCIALE":"COMMERCIAL PROPOSAL"}</h1><div class="grid"><div class="kpi">${it?"Punti luce":"Luminaires"}<b>${number(quote.qty)}</b></div><div class="kpi">${it?"Prezzo progetto":"Project price"}<b>${money(quote.cashPrice)}</b></div><div class="kpi">${it?"Pagamento mensile":"Monthly payment"}<b>${money2(quote.monthly)}</b></div><div class="kpi">${it?"Beneficio netto annuo":"Annual net benefit"}<b>${money(quote.customerNet)}</b></div></div><h2>PowerAiD</h2><div class="note">${number(commercial.powerAidPerformanceSharePct,1)}% ${it?"del risparmio aggiuntivo dopo LED e CLO":"of additional saving after LED and CLO"}.<br><b>${money(quote.powerAidAnnualFee)}/${it?"anno":"year"}</b></div><h2>${it?"Cash flow cliente dopo il pagamento":"Customer cash flow after payment"}</h2><table><thead><tr><th>${it?"Anno":"Year"}</th><th>${it?"Risparmio energia":"Energy saving"}</th><th>PowerAiD</th><th>${it?"Manutenzione":"Maintenance"}</th><th>${it?"Pagamento":"Payment"}</th><th>${it?"Cash flow netto":"Net cash flow"}</th><th>${it?"Cumulato":"Cumulative"}</th></tr></thead><tbody>${rows}</tbody></table></div></body></html>`; const win=window.open("","_blank"); if(!win)return alert("Please allow pop-ups."); win.document.write(html); win.document.close(); setQuoteDialog(false); }

  const internalNav=[["overview","Overview"],["audit","Audit & Lighting"],["catalog","Product Catalogue"],["pricing","Price Management"],["finance","Finance"],["customer","Customer Case"],["internal","Internal Approval"],["settings","Project Assumptions"]];
  const customerNav=[["overview","Overview"],["audit","Audit & Lighting"],["finance","Finance"],["customer","Customer Case"]]; const nav=viewMode==="internal"?internalNav:customerNav;

  return <div style={s.shell}><aside style={s.sidebar}><div style={s.brand}><div style={s.logo}>V</div><div><b>VIMALUX</b><span>INTELLIGENCE</span></div></div><div style={s.projectBadge}><span>ACTIVE QUOTATION</span><b>{project.municipality}</b><small>{project.quotationId}</small></div><nav style={s.sideNav}>{nav.map(([id,label])=><button key={id} style={section===id?s.navActive:s.navButton} onClick={()=>setSection(id)}>{label}</button>)}</nav>{viewMode==="internal"&&<><button style={s.upload} onClick={()=>auditRef.current?.click()}>＋ Import audit</button><input hidden ref={auditRef} type="file" accept=".xlsx,.xls,.csv" onChange={importAudit}/></>}</aside>
  <main style={s.main}><header style={s.topbar}><div><span style={s.eyebrow}>QUOTATION WORKSPACE</span><h1>{project.municipality}</h1><p>Build and generate a customer-ready lighting offer.</p></div><div style={s.topActions}><button style={s.secondary} onClick={()=>setViewMode(viewMode==="internal"?"customer":"internal")}>{viewMode==="internal"?"Customer view":"Internal view"}</button><button style={s.primary} onClick={()=>setQuoteDialog(true)}>Generate quotation</button></div></header>
  <section style={s.kpis}><Kpi label="Luminaires" value={number(quote.qty)}/><Kpi label="Cash price" value={money(quote.cashPrice)}/><Kpi label="Monthly payment" value={money2(quote.monthly)}/><Kpi label="Customer net saving" value={money(quote.customerNet)}/><Kpi label="Energy reduction" value={`${number(calc.totals.energyReductionPct,1)}%`}/><Kpi label="CO₂ reduction" value={`${number(calc.finance.annualCo2Tonnes,1)} t`}/></section>
  {section==="overview"&&<div style={s.grid2}><Card title="Project setup"><div style={s.formGrid}><Field label="Municipality" value={project.municipality} onChange={v=>setProject(x=>({...x,municipality:v}))}/><Field label="Quotation ID" value={project.quotationId} onChange={v=>setProject(x=>({...x,quotationId:v}))}/></div></Card><Card title="Commercial summary"><Row label="Model" value={commercial.model}/><Row label="Contract period" value={`${commercial.years} years`}/><Row label="Financed total" value={money(quote.financedTotal)}/></Card></div>}
  {section==="audit"&&<Card title="Audit & lighting"><div style={s.tableWrap}><table style={s.table}><thead><tr><th>Group</th><th>Qty</th><th>Technology</th><th>Existing W</th><th>Selected luminaire</th><th>Smart</th><th>PowerAiD</th></tr></thead><tbody>{groups.map(g=><tr key={g.id}><td><input value={g.label} onChange={e=>updateGroup(g.id,"label",e.target.value)}/></td><td><input value={g.quantity} onChange={e=>updateGroup(g.id,"quantity",num(e.target.value))}/></td><td><select value={g.existingType} onChange={e=>updateGroup(g.id,"existingType",e.target.value)}><option>SAP</option><option>LED</option><option>MH</option><option>Mercury</option><option>Unknown</option></select></td><td><input value={g.existingWatt} onChange={e=>updateGroup(g.id,"existingWatt",num(e.target.value))}/></td><td><select value={g.productId} onChange={e=>updateGroup(g.id,"productId",e.target.value)}>{activeProducts.map(p=><option key={p.id} value={p.id}>{p.name} · {p.watt}W</option>)}</select><button style={s.smallButton} onClick={()=>applyRecommendation(g.id)}>Apply AI recommendation</button></td><td><input type="checkbox" checked={g.smart} onChange={e=>updateGroup(g.id,"smart",e.target.checked)}/></td><td><input type="checkbox" checked={g.powerAid} disabled={!g.smart} onChange={e=>updateGroup(g.id,"powerAid",e.target.checked)}/></td></tr>)}</tbody></table></div></Card>}
  {section==="catalog"&&<Card title="Product Catalogue"><div style={s.catalogHead}><p>Manage standard luminaires used by the recommendation and quotation engine.</p><button style={s.primary} onClick={addProduct}>＋ Add product</button></div><div style={s.tableWrap}><table style={s.table}><thead><tr><th>Product ID</th><th>Name</th><th>Category</th><th>Watt</th><th>Lumen</th><th>Cost price</th><th>Sales price</th><th>Active</th><th>Actions</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.id}</td><td><input value={p.name} onChange={e=>updateProduct(p.id,"name",e.target.value)}/></td><td><select value={p.category||"LED Streetlights"} onChange={e=>updateProduct(p.id,"category",e.target.value)}><option>LED Streetlights</option><option>Smart Lighting</option><option>Adaptive Lighting</option><option>CMS</option><option>Services</option></select></td><td><input inputMode="decimal" value={p.watt} onChange={e=>updateProduct(p.id,"watt",num(e.target.value))}/></td><td><input inputMode="decimal" value={p.lumen} onChange={e=>updateProduct(p.id,"lumen",num(e.target.value))}/></td><td><input inputMode="decimal" value={p.buyPrice} onChange={e=>updateProduct(p.id,"buyPrice",num(e.target.value))}/></td><td><input inputMode="decimal" value={p.sellPrice} onChange={e=>updateProduct(p.id,"sellPrice",num(e.target.value))}/></td><td><input type="checkbox" checked={p.active!==false} onChange={e=>updateProduct(p.id,"active",e.target.checked)}/></td><td><button style={s.linkButton} onClick={()=>duplicateProduct(p.id)}>Duplicate</button><button style={s.dangerButton} onClick={()=>removeProduct(p.id)}>Delete</button></td></tr>)}</tbody></table></div></Card>}
  {section==="pricing"&&<><Card title="Price Management"><div style={s.formGrid3}><Field numeric label="Installation cost / lamp" value={commercial.installationCostPerLamp} onChange={v=>updateCommercial("installationCostPerLamp",num(v))}/><Field numeric label="Installation sales / lamp" value={commercial.installationSellPerLamp} onChange={v=>updateCommercial("installationSellPerLamp",num(v))}/><Field numeric label="Freight & duty cost / lamp" value={commercial.freightCostPerLamp} onChange={v=>updateCommercial("freightCostPerLamp",num(v))}/><Field numeric label="Freight & duty sales / lamp" value={commercial.freightSellPerLamp} onChange={v=>updateCommercial("freightSellPerLamp",num(v))}/><Field numeric label="Smart hardware cost / lamp" value={commercial.smartHardwareCostPerLamp} onChange={v=>updateCommercial("smartHardwareCostPerLamp",num(v))}/><Field numeric label="Smart hardware sales / lamp" value={commercial.smartHardwareSellPerLamp} onChange={v=>updateCommercial("smartHardwareSellPerLamp",num(v))}/><Field numeric label="CMS cost / lamp / year" value={commercial.cmsCostPerLampYear} onChange={v=>updateCommercial("cmsCostPerLampYear",num(v))}/><Field numeric label="CMS sales / lamp / year" value={commercial.cmsSellPerLampYear} onChange={v=>updateCommercial("cmsSellPerLampYear",num(v))}/><Field numeric label="PowerAiD share of extra saving %" value={commercial.powerAidPerformanceSharePct} onChange={v=>updateCommercial("powerAidPerformanceSharePct",num(v))}/><Field numeric label="PowerAiD internal cost / lamp / year" value={commercial.powerAidCostPerLampYear} onChange={v=>updateCommercial("powerAidCostPerLampYear",num(v))}/><Field numeric label="Agent commission %" value={commercial.commissionPct} onChange={v=>updateCommercial("commissionPct",num(v))}/><Field numeric label="Bonus %" value={commercial.bonusPct} onChange={v=>updateCommercial("bonusPct",num(v))}/></div></Card><Card title="PowerAiD performance pricing"><Row label="Calculation basis" value="Additional saving after LED + CLO"/><Row label="PowerAiD extra saving" value={`${number(calc.totals.powerAidSavingKwh)} kWh/year`}/><Row label="Gross extra saving" value={money(quote.powerAidAnnualGrossSaving)}/><Row label={`VIMALUX share (${number(commercial.powerAidPerformanceSharePct,1)}%)`} value={money(quote.powerAidAnnualFee)} highlight/></Card><Card title="Cost & pricing engine"><PriceTable rows={pricingRows(quote)}/></Card></>}
  {section==="finance"&&<div style={s.grid2}><Card title="Financing"><div style={s.formGrid}><Select label="Model" value={commercial.model} options={["Noleggio Operativo","Cash purchase","Lighting as a Service","ESCO","PPP"]} onChange={v=>updateCommercial("model",v)}/><Field numeric label="Period (years)" value={commercial.years} onChange={v=>updateCommercial("years",num(v))}/><Field numeric label="Customer interest %" value={commercial.interestRate} onChange={v=>updateCommercial("interestRate",num(v))}/><Field numeric label="Upfront payment" value={commercial.upfront} onChange={v=>updateCommercial("upfront",num(v))}/></div></Card><Card title="Payment result"><div style={s.heroNumber}>{money2(quote.monthly)}<span>per month</span></div><Row label="Annual payment" value={money(quote.annualPayment)}/><Row label="Financed total" value={money(quote.financedTotal)}/><Row label="Total interest" value={money(quote.interest)}/></Card></div>}
  {section==="customer"&&<><div style={s.grid2}><Card title="Customer business case"><Row label="Energy saving / year" value={money(calc.totals.totalEnergySavingValue)}/><Row label="PowerAiD fee / year" value={`− ${money(quote.powerAidAnnualFee)}`}/><Row label="Maintenance saving / year" value={money(quote.maintenanceSaving)}/><Row label="Annual payment" value={`− ${money(quote.annualPayment)}`}/><Row label="Net benefit / year" value={money(quote.customerNet)} highlight/></Card><Card title="Energy & CO₂"><Row label="Baseline consumption" value={`${number(calc.totals.baselineKwh/1000)} MWh`}/><Row label="Final consumption" value={`${number(calc.totals.residualGridKwh/1000)} MWh`}/><Row label="CO₂ reduction / year" value={`${number(calc.finance.annualCo2Tonnes,1)} t`}/></Card></div><Card title="Customer cash flow after payment"><CashflowTable rows={quote.yearly}/></Card></>}
  {section==="internal"&&<><section style={s.approvalHero}><div><span>APPROVAL STATUS</span><b>{quote.mol3>0?"APPROVABLE":"REVIEW REQUIRED"}</b></div><ApprovalMetric label="Revenue" value={money(quote.cashPrice)}/><ApprovalMetric label="Direct cost" value={money(quote.totalCost)}/><ApprovalMetric label="Gross margin" value={money(quote.grossMargin)}/><ApprovalMetric label="MOL 3" value={money(quote.mol3)}/></section></>}
  {section==="settings"&&<div style={s.grid2}><Card title="Commercial assumptions"><div style={s.formGrid}><Field numeric label="Maintenance after / lamp / year" value={commercial.maintenanceAfterPerLamp} onChange={v=>updateCommercial("maintenanceAfterPerLamp",num(v))}/><Field numeric label="Finance sell-off cost %" value={commercial.financeSellOffPct} onChange={v=>updateCommercial("financeSellOffPct",num(v))}/><Field numeric label="Electricity price increase % / year" value={assumptions.savingIndexationPct} onChange={v=>setAssumptions(x=>({...x,savingIndexationPct:num(v)}))}/><Field numeric label="Energy price (€/kWh)" value={assumptions.energyPrice} onChange={v=>setAssumptions(x=>({...x,energyPrice:num(v)}))}/></div></Card><Card title="Technical assumptions"><div style={s.formGrid}><Field numeric label="Burning hours / year" value={assumptions.burningHours} onChange={v=>setAssumptions(x=>({...x,burningHours:num(v)}))}/><Field numeric label="CLO saving %" value={assumptions.cloSavingPct} onChange={v=>setAssumptions(x=>({...x,cloSavingPct:num(v)}))}/><Field numeric label="PowerAiD additional saving % after CLO" value={assumptions.powerAidAdditionalSavingPct} onChange={v=>setAssumptions(x=>({...x,powerAidAdditionalSavingPct:num(v)}))}/><Field numeric label="CO₂ kg / kWh" value={assumptions.co2KgPerKwh} onChange={v=>setAssumptions(x=>({...x,co2KgPerKwh:num(v)}))}/></div></Card></div>}
  </main>{quoteDialog&&<div style={s.modalBackdrop}><div style={s.modal}><h2>Generate quotation</h2><Select label="Language" value={quoteLanguage} options={["it","en"]} onChange={setQuoteLanguage}/><div style={s.modalActions}><button style={s.secondary} onClick={()=>setQuoteDialog(false)}>Cancel</button><button style={s.primary} onClick={generateQuotation}>Generate quotation</button></div></div></div>}</div>;
}

const pricingRows=q=>[["LED luminaires",q.lumCost,q.lumSell],["CityManager hardware",q.smartCost,q.smartSell],["CMS, connectivity & support",q.cmsCost,q.cmsSell],["PowerAiD performance service",q.powerCost,q.powerSell],["Installation",q.installCost,q.installSell],["Freight & duty",q.freightCost,q.freightSell]];
function Kpi({label,value}){return <div style={s.kpi}><span>{label}</span><b>{value}</b></div>}
function Card({title,children}){return <section style={s.card}><h2>{title}</h2><div style={{marginTop:16}}>{children}</div></section>}
function Field({label,value,onChange,numeric}){return <label style={s.field}><span>{label}</span><input inputMode={numeric?"decimal":"text"} value={value??""} onChange={e=>onChange(e.target.value)}/></label>}
function Select({label,value,onChange,options}){return <label style={s.field}><span>{label}</span><select value={value} onChange={e=>onChange(e.target.value)}>{options.map(o=><option key={o}>{o}</option>)}</select></label>}
function Row({label,value,highlight}){return <div style={highlight?{...s.row,...s.rowHighlight}:s.row}><span>{label}</span><b>{value}</b></div>}
function ApprovalMetric({label,value}){return <div style={s.approvalMetric}><span>{label}</span><b>{value}</b></div>}
function PriceTable({rows}){return <div style={s.tableWrap}><table style={s.table}><thead><tr><th>Component</th><th>Cost</th><th>Sales price</th><th>Margin</th><th>Margin %</th></tr></thead><tbody>{rows.map(([n,c,sell])=>{const m=sell-c;return <tr key={n}><td><b>{n}</b></td><td>{money(c)}</td><td>{money(sell)}</td><td>{money(m)}</td><td>{sell?`${number(m/sell*100,1)}%`:"–"}</td></tr>})}</tbody></table></div>}
function CashflowTable({rows}){return <div style={s.tableWrap}><table style={s.table}><thead><tr><th>Year</th><th>Energy saving</th><th>PowerAiD fee</th><th>Maintenance</th><th>Customer payment</th><th>Net cash flow</th><th>Accumulated</th></tr></thead><tbody>{rows.map(r=><tr key={r.year}><td>{r.year}</td><td>{money(r.indexedEnergy)}</td><td>− {money(r.powerAidFee)}</td><td>{money(r.maintenanceSaving)}</td><td>− {money(r.customerPayment)}</td><td>{money(r.netCashflow)}</td><td>{money(r.cumulative)}</td></tr>)}</tbody></table></div>}
const s={shell:{minHeight:"100vh",display:"flex",background:"#f3f6f8",color:"#102a2e",fontFamily:"Segoe UI,Arial,sans-serif"},sidebar:{position:"fixed",inset:"0 auto 0 0",width:250,background:"#0d2b2f",color:"white",padding:20,minHeight:"100vh"},brand:{display:"flex",gap:11,alignItems:"center",paddingBottom:22},logo:{width:38,height:38,borderRadius:10,background:"#c9f15a",color:"#0d2b2f",display:"grid",placeItems:"center",fontWeight:900},projectBadge:{display:"grid",gap:5,padding:14,border:"1px solid #27494d",background:"#15373b",borderRadius:12,marginBottom:12},sideNav:{display:"grid",gap:5},navButton:{border:0,background:"transparent",color:"#b8c8ca",padding:"11px 12px",borderRadius:9,textAlign:"left",cursor:"pointer"},navActive:{border:0,background:"#c9f15a",color:"#102a2e",padding:"11px 12px",borderRadius:9,textAlign:"left",fontWeight:800,cursor:"pointer"},upload:{marginTop:18,padding:12,width:"100%",borderRadius:9,border:"1px dashed #5d777a",background:"transparent",color:"white"},main:{marginLeft:250,width:"calc(100% - 250px)",padding:"30px 34px 60px"},topbar:{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:24},eyebrow:{fontSize:11,fontWeight:800,letterSpacing:1.4,color:"#668084"},topActions:{display:"flex",gap:10},primary:{border:0,background:"#0d2b2f",color:"white",padding:"11px 15px",borderRadius:9,fontWeight:700,cursor:"pointer"},secondary:{border:"1px solid #cad6d8",background:"white",padding:"10px 14px",borderRadius:9,fontWeight:700,cursor:"pointer"},smallButton:{marginTop:5,border:"1px solid #cad6d8",padding:"7px 10px",borderRadius:7,background:"white",cursor:"pointer"},linkButton:{border:0,background:"transparent",color:"#16697a",cursor:"pointer",marginRight:8},dangerButton:{border:0,background:"transparent",color:"#b42318",cursor:"pointer"},kpis:{display:"grid",gridTemplateColumns:"repeat(6,minmax(130px,1fr))",gap:12,marginBottom:18},kpi:{background:"white",border:"1px solid #dfe7e8",borderRadius:13,padding:16,display:"grid",gap:7},card:{background:"white",border:"1px solid #dfe7e8",borderRadius:15,padding:20,marginBottom:16},grid2:{display:"grid",gridTemplateColumns:"repeat(2,minmax(0,1fr))",gap:16},formGrid:{display:"grid",gridTemplateColumns:"repeat(2,minmax(150px,1fr))",gap:13},formGrid3:{display:"grid",gridTemplateColumns:"repeat(3,minmax(150px,1fr))",gap:13},field:{display:"grid",gap:6,fontSize:12,fontWeight:700,color:"#52696c"},tableWrap:{overflowX:"auto"},table:{width:"100%",borderCollapse:"collapse",minWidth:760},row:{display:"flex",justifyContent:"space-between",gap:15,padding:"11px 0",borderBottom:"1px solid #edf1f2"},rowHighlight:{background:"#eff8d8",padding:"13px 12px",borderRadius:9,border:0,marginTop:7},heroNumber:{fontSize:34,fontWeight:850,color:"#0d2b2f",display:"grid",gap:5,marginBottom:12},approvalHero:{display:"grid",gridTemplateColumns:"1.2fr repeat(4,1fr)",gap:12,marginBottom:16},approvalMetric:{padding:17,border:"1px solid #dfe7e8",background:"white",borderRadius:13,display:"grid",gap:7},modalBackdrop:{position:"fixed",inset:0,background:"rgba(13,43,47,.58)",display:"grid",placeItems:"center",zIndex:30,padding:20},modal:{width:"min(650px,100%)",background:"white",borderRadius:16,padding:22},modalActions:{display:"flex",justifyContent:"flex-end",gap:10,marginTop:22},catalogHead:{display:"flex",justifyContent:"space-between",alignItems:"center",gap:16,marginBottom:14}};
const style=document.createElement("style");style.textContent=`*{box-sizing:border-box}body{margin:0}h1{margin:3px 0 5px;font-size:29px}h2{margin:0;font-size:20px}p{margin:0;color:#6e8285}.brand span{display:block;font-size:9px;color:#91a9ab;letter-spacing:.8px}.projectBadge span,.projectBadge small{font-size:10px;color:#91a9ab}.kpi span{font-size:13px}.kpi b{font-size:20px}input,select{width:100%;border:1px solid #cad6d8;border-radius:8px;padding:9px;background:white;color:#17363a}input[type=checkbox]{width:16px}th{text-align:left;font-size:10px;text-transform:uppercase;color:#708487;padding:10px;border-bottom:1px solid #dce5e6}td{padding:10px;border-bottom:1px solid #edf1f2;font-size:12px}@media(max-width:1100px){.kpis{grid-template-columns:repeat(3,1fr)!important}.formGrid3{grid-template-columns:repeat(2,1fr)!important}}`;if(typeof document!=="undefined"&&!document.getElementById("vimalux-main-css")){style.id="vimalux-main-css";document.head.appendChild(style);}